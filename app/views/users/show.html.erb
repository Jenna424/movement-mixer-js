<h3><%= @user.name %>'s Profile Page</h3>

<p>
	<strong>Workout Count</strong>: 
	<%= @user.name %> designed <%= @user.routines.count %> workout <%= 'routine'.pluralize(@user.routines.count) %> to date!
</p>

<%= link_to "View #{@user.name}'s Workouts", user_path(@user), class: "user-workouts", data: {id: @user.id } %>

<br><br>