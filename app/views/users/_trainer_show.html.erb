<p>
  <strong>Training Guides Contributed</strong>: 
  <%= user.name %> published <%= user.guides.count %> training <%= 'guide'.pluralize(user.guides.count) %> for performing exercises.
</p>

<%= link_to "View Training Guides by #{user.name}", user_path(user), class: "load-user-guides", data: {id: user.id } %>